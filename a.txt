// struct iphdr *iph = (struct iphdr *)(packet_body +  sizeof(struct ethhdr));
                // unsigned short iphdrlen = iph->ihl*4;
                // struct udphdr *udph = (struct udphdr*)(packet_body + iphdrlen  + sizeof(struct ethhdr));
                // int header_size =  sizeof(struct ethhdr) + iphdrlen + sizeof udph;
                // check = packet_body +  header_size;

                // printf("%d - %d - %d - %d", *check, *(check + 1), *(check + 2), *(check + 3));


                // check protocol properties
                //check_properties(protocol, check, packet_header);